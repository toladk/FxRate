import { TransactionfilterPipe } from './transactionfilter.pipe';

describe('TransactionfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
